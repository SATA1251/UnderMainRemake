using System.Collections;
using System.Collections.Generic;
//using UnityEditor.PackageManager;
using UnityEngine;

public class AttackCollider : MonoBehaviour
{
/*    [SerializeField] private HandController hController;
    [SerializeField] private EnemyDamage enemyDamage;
    [SerializeField] private WallScript wallScript;
    //[SerializeField] private HandController hController;

    void Start()
    {
        // hController = GameObject.FindGameObjectWithTag("Player").GetComponent<HandController>();
        hController = GameObject.Find("Holder").GetComponent<HandController>();

    }
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag == "Enemy")
        {
            enemyDamage = GameObject.FindGameObjectWithTag("Enemy").GetComponent<EnemyDamage>();
            enemyDamage.EnemyHit();
            //audioSrc.PlayOneShot(moleHit);
        }
        if (other.gameObject.tag == "Enemy2")
        {
            GetComponent<EnemyDamage>().EnemyHit2();

        }
        if (other.gameObject.tag == "Enemy3")
        {
            GetComponent<EnemyDamage>().EnemyHit3();

        }
        if (other.gameObject.tag == "Wall")
        {
            // 벽 부수기
            //GetComponent<WallScript>().DestroyWall();
            //audioSrc.PlayOneShot(wallHit);
            wallScript = GameObject.FindGameObjectWithTag("Wall").GetComponent<WallScript>();
            wallScript.DestroyWall();
        }
    }*/
}
